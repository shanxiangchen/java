<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.mapper.BankCodeTypeMapper">
 	
	<select id="selectBankCodeTypePageList" resultType="com.app.entity.BankCodeType" parameterType="com.app.entity.BankCodeType">
         select
	       b.code_type_id as codeTypeId,
	       b.code_type_name as codeTypeName,
	       b.sort_no as sortNo,
	       b.remark as remark
	    from bank_code_type b
         where 1=1
		<if test="codeTypeId!=null and codeTypeId!=''">
		  and  b.code_type_id like '%${codeTypeId}%'
	    </if>
	    <if test="codeTypeName!=null and codeTypeName!=''">
	     and  code_type_name like '%${codeTypeName}%'		
		</if> 
	</select> 
	 
	<select id="selectCountByCodeTypeId"  resultType="int">
		select 
		  count(1)
		from bank_code_type a
		where a.code_type_id=#{codeTypeId,jdbcType=VARCHAR}
	</select>
	<insert id="saveBankCodeType" parameterType="com.app.entity.BankCodeType">
	    insert into bank_code_type (code_type_id,code_type_name,sort_no,remark) 
	    values(#{codeTypeId},#{codeTypeName,jdbcType=VARCHAR},#{sortNo,jdbcType=VARCHAR},#{remark,jdbcType=VARCHAR})
	</insert>
	
	<select id="getBankCodeTypeServiceById" parameterType="java.lang.String" resultType="com.app.entity.BankCodeType">
	    select 
		   b.code_type_id as codeTypeId,
	       b.code_type_name as codeTypeName,
	       b.sort_no as sortNo,
	       b.remark as remark
		from  bank_code_type b
		where b.code_type_id=#{codeTypeId,jdbcType=VARCHAR}
	</select>
	
	<update id="updateBankCodeType" parameterType="com.app.entity.BankCodeType">
	    update bank_code_type b set
	    b.code_type_name=#{codeTypeName,jdbcType=VARCHAR},
	    b.remark=#{remark,jdbcType=VARCHAR}
	   where b.code_type_id=#{codeTypeId,jdbcType=VARCHAR}
	</update>
	
	<delete id="deleteCodeType">
	    delete from bank_code_type 
		<where>
			code_type_id  in
			<foreach collection="array" item="item" index="index" open="("
				separator="," close=")">#{item}</foreach>
		</where>
		 
	</delete>
	<delete id="deleteCodeTypeInfoById">
	    delete from bank_code_type 
		<where>
			code_type_id  in
			<foreach collection="array" item="item" index="index" open="("
				separator="," close=")">#{item}</foreach>
		</where>
	 
	</delete>
	
	 
	
	 
</mapper>